[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "within-mcp-factory"
version = "0.0.0"
description = "Manifest factory for Within MCP plugin distribution (Claude Code plugin + Gemini CLI extension)."
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.11"
authors = [
    { name = "Klarity Intelligence, Inc.", email = "hello@within.ai" },
]
keywords = ["within", "mcp", "claude-code", "gemini-cli", "plugin"]
classifiers = [
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
]
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8", "pytest-cov>=5"]

[project.urls]
Homepage = "https://www.within.ai/"
Repository = "https://github.com/Within/within-mcp"

[project.scripts]
within-mcp-generate = "within_mcp.cli:main"

[tool.setuptools.packages.find]
include = ["within_mcp*"]
exclude = ["tests*"]
