interface:
  display_name: Agent Builder
  short_description: Turn how your organization works into skills, agents, and automations.
  default_prompt: Find automation opportunities in my finance team's processes.
  example_prompts:
    - Find automation opportunities in my finance team's processes.
    - Turn how our contracts team reviews agreements into a reusable skill.
    - What could Within build for my team?
allow_implicit_invocation: true
dependencies:
  tools:
    - get_agent_builder_instructions
    - get_agent_builder_resource
